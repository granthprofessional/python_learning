'''collection = {1,2,3,4, "hello", "world" , "world", 5}

print(collection)
print(len(collection)) #total no. of items, only added a unique vale not a doubbled value '''

null_set = set() #empty set; syntax
# only use a peryinthesis not a curly braces
print(type(null_set))